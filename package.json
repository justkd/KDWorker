{
  "name": "kdworker",
  "version": "1.1.0",
  "description": "KDWorker creates web workers on the fly. Simply pass the web worker function and its parameter to KDWorker and it will build the web worker script, add it to the DOM, run the web worker, and finally revoke the DOMString when finished.",
  "keywords": [],
  "main": "src/index.js",
  "dependencies": {
    "@material-ui/core": "4.11.3",
    "@material-ui/icons": "4.11.2",
    "react": "17.0.1",
    "react-dom": "17.0.1"
  },
  "devDependencies": {
    "@types/node": "14.14.6",
    "typescript": "4.0.5",
    "prettier": "2.1.2"
  },
  "scripts": {
    "lint": "eslint './src/**/*.{ts,tsx}'",
    "lint:fix": "eslint './src/**/*.{ts,tsx}' --fix"
  },
  "browserslist": [">0.2%", "not dead", "not ie <= 11", "not op_mini all"],
  "license": "MIT"
}
